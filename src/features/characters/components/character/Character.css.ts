import { style } from '@vanilla-extract/css';

export const avatarContainer = style({
	blockSize: '100%',
	backgroundColor: 'color-mix(in hsl longer hue, hsl(120deg 18.73% 88.15%) 20%, Canvas)',
});

export const fullHeight = style({
	blockSize: '100%',
});
