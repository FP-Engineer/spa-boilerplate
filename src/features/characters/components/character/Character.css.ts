import { style } from '@vanilla-extract/css';

export const avatarContainer = style({
	blockSize: '100%',
	backgroundColor: 'color-mix(in hsl longer hue, hsl(120deg 20% 60%) 20%, Canvas)',
});

export const fullHeight = style({
	blockSize: '100%',
});
